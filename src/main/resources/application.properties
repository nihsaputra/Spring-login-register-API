server.port=8081
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/login_register_db
spring.datasource.username=root
spring.datasource.password=@Saputra03

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

# Account SuperAdmin
app.login-register.email=${EMAIL:superadmin@gmail.com}
app.login-register.passaword=${PASSWORD:password}

# JWT
app.login-register.app-name=${APP-NAME:login-register}
app.login-register.jwt-secret=${JWT-SECRET:secret}
app.login-register.jwt-expirationInSecond=${EXPIRATION-IN-SECOND:86400}